[package]
name = "alloc_data"
version = "0.1.0"
authors = ["Adrien Champion <adrien.champion@email.com>"]
edition = "2018"

[dependencies]
chrono = { version = "*", features = ["serde", "wasmbind"] }
error-chain = "*"
lazy_static = "*"
num-bigint = { version = "*", features = ["serde"] }
num-traits = "*"
serde = "*"
serde_derive = "*"
toml = "*"

swarkn = { git = "https://github.com/AdrienChampion/swarkn" }

base = { path = "../base" }